// DashboardHeader is no longer used — the sidebar layout handles
// the header, navigation, and logout. This file is kept as a no-op
// to avoid breaking any imports, but can be deleted safely.
export function DashboardHeader() {
  return null;
}
